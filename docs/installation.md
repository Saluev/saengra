# Installation

Saengra is available on PyPI and can be installed with your basic `pip install`:

```shell
python -m pip install saengra
```

Compiled PyPI packages are available for Linux and MacOS. Other platforms
would require compiling from source:

```shell
git clone git@github.com:Saluev/saengra.git
cd saengra
python -m pip install .
```
